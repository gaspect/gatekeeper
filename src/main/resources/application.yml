spring:
    
    application:
        name: gateway
    security:
        oauth2:
            resourceserver:
                jwt:
                    jwk-set-uri: ${JWK_URI}
    cloud:
        gateway:
            routes:
            - id: pacs
              uri: ${API_URI}
              predicates:
                - Path=/**

server:
        port: 8080